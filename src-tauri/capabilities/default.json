{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the menu bar monitor app",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
